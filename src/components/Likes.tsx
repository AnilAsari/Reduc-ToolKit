import { useSelector } from "react-redux"
import { RootState } from "../Redux/store"

const Likes=()=>{


    // const likes=useSelector((state:RootState)=>state.counter.value);

    return(
        <div>
            {/* Likes:{likes} */}
        </div>

    )
};


export default Likes;